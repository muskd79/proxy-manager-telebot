{
  "name": "proxy-manager-rate-limiter",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object token-bucket rate limiter fronting vendor API calls",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.2",
    "wrangler": "^3.100.0"
  }
}

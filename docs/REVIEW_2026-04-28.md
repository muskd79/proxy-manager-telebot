# REVIEW Kiến trúc proxy-manager-telebot — 2026-04-28

## 1. Sơ đồ kiến trúc

```
proxy-manager-telebot/  (Next.js 16.2 + React 19 + grammy 1.41 + Supabase 2.101)
├── src/
│   ├── app/                           # App Router
│   │   ├── (auth)/                    # login, forgot/reset password
│   │   ├── (dashboard)/               # 18 admin pages
│   │   ├── auth/recover-2fa/
│   │   └── api/                       # 48 route handlers
│   │       ├── auth/                  # callback, recover-2fa, track-login
│   │       ├── proxies/               # CRUD, bulk-edit, import, export, probe[-batch], stats, check
│   │       ├── users/                 # CRUD, [id]/proxies
│   │       ├── admins/                # CRUD, reset-password, disable-2fa, revoke-sessions
│   │       ├── requests/              # GET list, [id] approve/reject (534 LOC monster)
│   │       ├── categories/            # CRUD, reorder, bulk-assign, defaults
│   │       ├── profile/               # email, password, 2fa enroll/verify/disable, sessions
│   │       ├── stats/                 # dashboard + analytics
│   │       ├── chat/                  # admin↔user chat
│   │       ├── logs/, settings/, health/, docs/, bot-simulator/
│   │       ├── cron/                  # 5 endpoints (cleanup, expire-proxies, expire-requests,
│   │       │                          #   health-check, expiry-warning) — Vercel cron
│   │       └── telegram/webhook/      # 191 LOC — secret + IP whitelist + 2-layer dedup
│   ├── lib/                           # No services/ layer — anemic
│   │   ├── auth.ts (212), validations.ts (261), proxy-detect.ts (301)
│   │   ├── csv.ts, csrf.ts, uuid7.ts, ip.ts, backup-codes.ts, rate-limiter.ts (186)
│   │   ├── supabase/{client,server,admin,middleware}.ts
│   │   ├── state-machine/{create-machine,proxy,request}.ts
│   │   ├── cron/advisory-lock.ts
│   │   ├── security/public-ip.ts (210)
│   │   └── telegram/                  # bot.ts, handlers.ts (229), 14 commands
│   ├── components/                    # 50+ UI (21 shadcn primitives + feature folders)
│   ├── hooks/, types/, locales/, __tests__/setup/
└── supabase/migrations/               # 40 SQL files (Wave 1 → Wave 22S)
```

**Data flow:**
```
Telegram User ──webhook──▶ /api/telegram/webhook (secret + IP + dedup + rate)
                              └─▶ grammy bot.handle ─▶ commands/* ─▶ supabaseAdmin
Admin Web    ──Supabase SSR──▶ /api/* (requireMinRole) ─▶ supabaseAdmin ─▶ Postgres
                                                          └─▶ logActivity ─▶ activity_logs
Vercel Cron  ──Bearer──▶ /api/cron/* (advisory-lock) ────▶ supabaseAdmin
```

## 2. Top 10 file rủi ro nhất

| # | Path | LOC | Vấn đề |
|---|------|-----|--------|
| 1 | `src/app/api/docs/openapi.ts` | 1213 | Spec 1 file static, không sync route |
| 2 | `src/app/api/__tests__/cron.test.ts` | 1081 | Test file > 800 LOC |
| 3 | `src/app/(dashboard)/profile/page.tsx` | 838 | God-component: 2FA + email + password + sessions + backup codes |
| 4 | `src/components/proxies/proxy-import.tsx` | 767 | Import UI phình to |
| 5 | `src/app/(dashboard)/bot/simulator/page.tsx` | 625 | Bot simulator client-side |
| 6 | `src/app/api/requests/[id]/route.ts` | 604 | Bulk approve + single approve + reject + restore + Telegram fanout |
| 7 | `src/app/(dashboard)/settings/page.tsx` | 603 | Settings + admin list + secret keys |
| 8 | `src/app/(dashboard)/proxies/page.tsx` | 567 | Filters + table + bulk-edit + form + import + export |
| 9 | `src/app/(dashboard)/admins/[id]/page.tsx` | 523 | Admin detail + reset-password + disable-2fa |
| 10 | `src/app/(dashboard)/admins/page.tsx` | 453 | Admin CRUD + role select |

## 3. Top 10 nợ kỹ thuật

| # | Mức | Issue | Path | Action |
|---|-----|-------|------|--------|
| 1 | CRITICAL | Vắng `src/services/` layer — business logic dồn vào route handlers | `src/app/api/**/route.ts` | Port pattern VIA: tạo `services/{requests,proxies,users,admins,categories}.service.ts` |
| 2 | CRITICAL | Bot admin commands check role qua `getAdminByTelegramId` riêng — không đồng nhất với web `requireMinRole` | `src/lib/telegram/commands/admin-approve.ts:33` | Đồng nhất gate, thêm integration test |
| 3 | CRITICAL | OpenAPI 1213 LOC static, drift với route thực | `src/app/api/docs/openapi.ts` | Generate từ Zod (`zod-to-openapi`), CI gate |
| 4 | HIGH | 163 lần `console.*` trong production (78 files) — vi phạm coding rule | toàn bộ src | Replace bằng `lib/logger.ts`, hook PostToolUse warn |
| 5 | HIGH | proxies.username/password lưu plaintext. Mig 020 prep pgsodium nhưng chưa apply | `migrations/020_wave19_pgsodium.sql` | Apply pgsodium + backfill |
| 6 | HIGH | i18n flat 1 file (313 LOC mỗi locale) | `src/lib/i18n.tsx` + `locales/*.json` | Tách domain (proxies/i18n.ts, requests/i18n.ts) |
| 7 | HIGH | Không có audit module riêng — chỉ logger flat | `src/lib/logger.ts` | Port `services/audit/{actions,redactor,query}.ts` từ VIA |
| 8 | HIGH | `requests/[id]/route.ts` saga ngầm: rate-limit + RPC + log + Telegram fanout không retry, không idempotent | `src/app/api/requests/[id]/route.ts:97-310` | State machine + outbox table |
| 9 | MEDIUM | 17 `: any` type leak (chủ yếu test, có cả production `proxies/page.tsx:310`) | grep results | Replace bằng `unknown` + Zod narrow |
| 10 | MEDIUM | Vendor schema mig 019-022 đã apply nhưng không có client/adapter code | `migrations/019_*` | Quyết định: rollback hoặc finish Wave 19/20 |

## 4. Heatmap test coverage

| Module | Files | Test files | Đánh giá |
|--------|-------|-----------|----------|
| `lib/` core utils | 24 | 14 | TỐT |
| `lib/state-machine/` | 3 | 1 | OK |
| `lib/cron/` | 1 | 1 | OK |
| `lib/security/` | 1 | 1 | OK |
| `lib/supabase/` | 4 | 1 | THIẾU middleware/server/client |
| `lib/telegram/` | 18 | 9 | TRUNG BÌNH |
| `app/api/` | 48 routes | 18 | TRUNG BÌNH — chat, logs, stats, settings, users, profile UI, proxies probe/import/export, categories reorder/bulk untested |
| `app/(dashboard)/` UI | 18 pages | 1 | KÉM |
| `components/` | 50+ | 1 | KÉM |

**Tổng**: dưới ngưỡng 80% rule. Không có Playwright E2E.

## 5. So sánh với sibling VIA project

VIA tại `C:\Users\Admin\Documents\quản lý via, giao via và gửi via qua bot tele` — 165 migrations vs proxy 40.

| Feature | VIA | Proxy | Gap |
|---------|-----|-------|-----|
| Service layer | 30+ files | KHÔNG | **CRITICAL** |
| Multi-tenant (org_id) | mig 056-068 | KHÔNG | Cần nếu SaaS |
| pgsodium encryption | mig 054 applied | mig 020 prepped, KHÔNG apply | HIGH |
| Audit subsystem | `services/audit/*` + retention cron | logger flat | HIGH |
| Domain i18n | `lib/bot/i18n/{myvia,status,whitelist,checkuid}.ts` | 1 file flat | MEDIUM |
| API request logs | mig 069 | KHÔNG | MEDIUM |
| Settings changelog | mig 070 | KHÔNG | MEDIUM |
| Warranty system | có | KHÔNG | Cần nếu sell proxy |
| FTS search | mig 033, 071 | KHÔNG | MEDIUM khi 100k+ |
| Materialized view | mig 032, 033, 035 | KHÔNG | MEDIUM khi scale |
| Blacklist recall | có | KHÔNG | Cần nếu chống fraud |
| File delivery via bot | mig 024, 040 | text only | OK |

## 6. 5 cải tiến lớn (3 tháng tới)

| # | Cải tiến | ROI | Effort |
|---|---------|-----|--------|
| 1 | Trích xuất service layer từ VIA pattern. `requests/[id]/route.ts` 604 LOC → 80 LOC | RẤT CAO | L (3-4 tuần) |
| 2 | Quyết định Wave 19/20 vendor — finish saga adapter HOẶC rollback | CAO | M-L (2-4 tuần) |
| 3 | Apply pgsodium + audit module + retention cron | CAO | M (1-2 tuần) |
| 4 | OpenAPI auto-gen từ Zod + CI gate | TRUNG BÌNH | S (3-5 ngày) |
| 5 | Playwright E2E + tách 5 god-pages >400 LOC | CAO | L (3-4 tuần) |

## 7. Quick wins (<1 ngày mỗi cái)

- 163 `console.*` → `logger.ts`
- Drop 17 `any` types
- Tách `app/api/docs/openapi.ts` per-tag
- Đổi `CategoryFormDialog.tsx` về kebab-case
- Add `vitest --coverage` script
- Cron health-check expose lock-acquire-fail metric
